---
title: 内置工具与技能
description: SLATE 自带的全部工具清单、调用方式与扩展方法。
section: 对话与自动化
order: 8
---

这一页是能力清单：SLATE 自带哪些工具、模型怎么用它们、哪些能关掉，以及怎么用 `SKILL.md` 和外部 MCP Server 把能力接得更多。

## 两类能力先分清

- **内置工具**：SLATE 自带的 Python 能力，开箱即用，不需要配置也不用安装。文件读写、跑命令、生成文档、联网搜索都在这一类。
- **Skill**：由 `SKILL.md` 定义的外部知识与流程。你自己写、上传或从本地路径导入，用来告诉模型"这件事该按什么步骤做"。

内置工具是"手"，Skill 是"做法"。两者都能被模型按需使用，也都能用 `@` 显式点名。

## 内置工具总览

| 类别 | 工具 | 用途 |
|---|---|---|
| 文件与代码 | `file_tree` | 浏览目录：可递归、按通配符过滤、快速扫描 |
| 文件与代码 | `file_peek` | 读取文件内容：自动识别多种编码，可按行范围或读取末尾 |
| 文件与代码 | `file_create` | 创建新文件 |
| 文件与代码 | `file_edit` | 差异预览式编辑：精确替换、按行号替换、插入、删除、复制与粘贴 |
| 文件与代码 | `file_append` | 向已有文件末尾追加，用于超长文件分段续写 |
| 文件与代码 | `code_search` | 项目内全局代码搜索：文本或正则，可缩小到子目录 |
| 文件与代码 | `repo_stats` | 项目文件类型、体积与数量统计 |
| 文件与代码 | `todo_scan` | 扫描项目里的 TODO / FIXME 待办标记 |
| 文件与代码 | `python_api_extract` | 提取 Python 库的公共 API 文档（签名、类方法、属性） |
| 终端与后台任务 | `terminal` | 受限沙箱命令执行，支持多会话与跨命令状态保持 |
| 终端与后台任务 | `bg_task` | 后台终端任务：起了就走，结束时叫醒模型 |
| 办公文档 | `doc_write` | 生成 Markdown 技术文档或需求说明 |
| 办公文档 | `ppt_create` | 生成 `.pptx` 演示文稿 |
| 办公文档 | `word_create` | 生成 `.docx` Word 文档 |
| 办公文档 | `excel_tool` | 生成 `.xlsx`、读取表格、csv 与 xlsx 互转 |
| 办公文档 | `pdf_tool` | PDF 元信息、文本与表格提取 |
| 办公文档 | `html_render` | 生成 HTML 骨架 |
| 办公文档 | `html_bundle` | 把网页的 css / js 内联合并成单个 html，便于分发 |
| 数据与图表 | `json_tool` | JSON 校验、格式化、压缩与按路径读取 |
| 数据与图表 | `regex_test` | 正则表达式测试与匹配结果预览 |
| 数据与图表 | `text_summarize` | 文本摘要与关键词提取 |
| 数据与图表 | `chart_create` | 生成 SVG 图表：柱状 / 条形 / 折线 / 饼图 |
| 数据与图表 | `qrcode_create` | 生成 SVG 二维码 |
| 搜索与网页 | `web_search` | 联网搜索，免 Key |
| 搜索与网页 | `web_fetch` | 获取网页正文，支持 JS 渲染页面与 PDF |
| 代码与文档安全 | `code_scan` | 代码安全扫描：硬编码密钥、SQL 注入、XSS、弱加密、调试残留 |
| 代码与文档安全 | `doc_scan` | 文档安全扫描：PII、凭证、财务数据、机密标记、内网信息 |
| Git | `git_tool` | Git 仓库只读信息：分支状态、提交日志、变更统计、分支与远程列表 |
| 系统与环境 | `system_info` | 系统元认知：日期时间、硬件配置、电量、网络状态 |
| 系统与环境 | `css_color` | 生成 CSS 配色方案 |
| 自动化 | `browser_automation` | 浏览器自动化：导航、截图、点击、输入、执行 JS |
| 自动化 | `computer_use` | 桌面自动化：鼠标键盘控制、窗口管理、图像定位 |
| 自动化 | `screenshot_to_code` | 截图转代码：分析截图并生成还原视觉的 HTML / CSS |
| 生成类 | `image_gen` | AI 图片生成，需在设置里配置模型与 API Key |
| 生成类 | `video_gen` | AI 视频生成，需在设置里配置模型与 API Key |
| MCP 工厂 | `mcp_factory` | 工具工厂：按描述自动生成新的工具 |

:::note 关于工具数量
上表共 36 条，覆盖你在工具面板与 `@` 提及候选里能看到的全部内置工具。其中 `file_append` 与 `file_create` 属于同一族：一个负责建、一个负责续，配合完成超长文件的分段写入。
:::

## 重点工具

### file_edit：改动先给你看

- 改动以 diff 形式呈现：文件名、`+N -M` 行数统计、红绿增删行，一屏看清动了什么。
- 卡片上有「接受」「拒绝」「复制 diff」三个按钮：接受才落盘，拒绝只是关掉这张卡（已写入的内容不会回滚）。
- 在「设置 → 上下文与项目」里勾选了「自动确认文件创建/修改」（默认勾选）时，预览没有报错的改动会直接写入，卡片显示「已自动应用」，不再给接受与拒绝。部分内容没命中时仍保留手动确认，避免只写进去半套。
- 编码安全：写入时保留原有的 UTF-8、BOM、GB18030、GBK、UTF-16 等编码，中文与 emoji 不会变成乱码。

### terminal：受限沙箱里跑命令

- 单条命令超时 30 秒，输出超过 5 万字符会被截断，命令长度也有上限。
- 支持多个终端会话并存，可以创建、列出、关闭，也可以终止会话里跑着的进程。
- Windows 下每条命令一个独立 PowerShell 进程：多行块（`foreach` / `if`）与 `&&` / `||` 都可用；`cd` 与 `$env:` 这类会话状态跨命令保持。
- 交互式命令行（裸跑 `python` / `node` 这类等待输入的 REPL）拿不到输入就会退出或拖到超时，不适合在这里用。
- 命令窗口不会弹到屏幕上，原生命令的输出捕获支持中文与 emoji。

### bg_task：后台终端任务的三层唤醒

编译、训练、起一个实验服务器这类以分钟计的活，交给它，不占住对话：

1. **主动问**：起完立刻返回任务标识、开头一小段输出与日志路径；之后用状态与日志查询去取新增输出。
2. **结束时叫醒**：起任务时带 `notify=true`，任务结束会把结果带着输出尾巴送回对话，模型被叫醒接着干。
3. **提前叫醒**：再挂一个正则，输出命中就提前唤醒，不必等命令跑完。

它是"起了就走"的一次性任务，不接受交互式输入；可以给超时让它到点自动终止，也可以随时停掉整棵进程树。任务寿命等于 SLATE 进程寿命——关掉 SLATE 任务就没了，日志留在本机可事后查。

:::warning 后台不等于免审批
后台任务走的审批档位与终端完全一致，高危命令照样会弹窗问你，灾难级命令直接拒绝。
:::

### 搜索、Git 与联网

- `code_search`：在项目里做全局代码搜索，支持文本与正则，默认从项目根开始，可以缩小到某个子目录。
- `git_tool`：只读——看分支与工作区状态、最近提交、变更统计、分支与远程列表。它不会替你做提交、推送这类写操作，那些命令由模型在终端里执行，同样受审批约束。
- `web_search`：免 Key 联网搜索，双引擎并发合并去重，取实时信息。
- `web_fetch`：抓取指定网页的标题、描述与正文，支持需要 JS 渲染的页面与 PDF。

## 工具怎么被调用

- **模型自主调用**：你直接描述需求（"把这份数据做成图表"），模型自己决定用哪个工具，不需要点名。
- **对话里能看到**：每笔调用都会变成一张工具卡，显示工具名、关键参数与执行状态，结果可展开查看。
- **高危命令要审批**：命令与联网访问都过审批门。审批档位有手动审批 / 自动审批 / 完全访问三档，本场可随时切换；灾难级命令在任何档位下都无条件拦截。规则见 [设置与安全审批](./19-settings.md)。
- **后台任务有独立面板**：正在跑的后台终端任务列在右栏任务中心，可以直接查看与停止。

## 工具的开关

「设置 → AI 辅助功能」是对话以外所有耗 Token 功能的开关。其中三项属于工具类：

- 子代理并行（`subagent_run`）
- AI 图片生成
- AI 视频生成

关掉这三项中的任何一项，对应工具会**从工具目录里消失**——模型看不到它，也就不会调用它。图片与视频生成还需要在「图片生成」「视频生成」区块单独配置模型与 API Key。

## 自定义 Skill

`SKILL.md` 即插即用。入口是「设置 → 工具 / 技能」，面板上半部分是内置工具（不可删除），下半部分是技能列表。

一份 Skill 的写法：

- 一个目录里放一个 `SKILL.md`，目录名就是技能名。
- 文件里写清这份技能的用途（`description:` 一行）与要遵循的步骤、约定、注意事项。
- 除了 `SKILL.md`，同一个目录里可以再放它需要的参考文档。

添加与删除：

- 「新建技能」上传文件创建，「导入技能」从本地路径导入（给包含 `SKILL.md` 的目录，或单个 `.md` 文件）。
- 「GitHub」从仓库导入，「发现插件」扫描本机已安装的 Codex / Claude 标准插件目录。
- 技能可以删除，删除后模型与 `@` 提及都不再看到它；内置工具不受影响。

## 接入外部 MCP 服务器

在「设置 → MCP Server」里点「＋ 添加 MCP Server」，填名称与地址即可接入外部 MCP 工具。列表会显示连接状态、它提供的工具数量与工具名，并提供连接、断开、删除。

接入后，它的工具会出现在工具目录与 `@` 提及候选里，模型可以像用内置工具一样调用。

**项目级覆盖**：打开一个项目后，列表里每台服务器会多出一个项目按钮，可以「在本项目停用」「在本项目单独启用」「摘掉本项目掩码」，也可以只放行其中几个工具。这类覆盖只影响当前项目，别的项目照旧；服务器的地址与密钥始终留在全局配置里，不会写进你的项目目录。详见 [项目、工作区与分项目配置](./14-projects.md)。

:::tip 在输入框里点名
想明确指定用哪个工具或技能，直接在输入框输入 `@` 从候选里挑，发送时会把它的定义注入这一条消息。`@` 的候选范围与用法见 [对话与消息](./05-chat.md)。
:::
